DomainHost is a ruby version of HostedBy.

This application parses an HTML Bookmarks file and produces a list of the domain hosts used by
the domains contained in the file. 

It required that Rails be installed, and the whois gem. It also expects to run on Ruby 1.9.3.